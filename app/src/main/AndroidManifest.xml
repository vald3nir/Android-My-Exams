<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:configChanges="orientation|keyboardHidden"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectsAndroid"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".presentation.features.boot.BootActivity"
            android:exported="true"
            android:theme="@style/Theme.ProjectsAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.features.profile.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.features.insertExam.InsertExamActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.features.home.ExamsHomeActivity"
            android:exported="false" />

    </application>

</manifest>